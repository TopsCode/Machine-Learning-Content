
def fullName(first, last):
    """
        This function takes two arguments and it return 
        combination of both arguments
        
        args:
            first = it takes string datatypes
            last = it also takes string datatypes
        this return combination of first + last
    """
    fullname = first + last
    return fullname
